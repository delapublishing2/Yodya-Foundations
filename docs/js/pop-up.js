// Open the popup
function openPopup() {
    const popup = document.getElementById('imagePopup');
    popup.style.display = 'flex'; // Show popup with flex layout
}

// Close the popup
function closePopup() {
    const popup = document.getElementById('imagePopup');
    popup.style.display = 'none'; // Hide the popup
}
